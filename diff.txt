commit 798734429c5f24b5a001474d6e1f3d54789edcce
Author: Admin <Admin@YPHP>
Date:   Wed Aug 19 23:28:48 2026 -0700

    fix sync and async mode

diff --git a/webfamily.client/src/app/shared/guards/admin.guard.ts b/webfamily.client/src/app/shared/guards/admin.guard.ts
index 6c53777..4542289 100644
--- a/webfamily.client/src/app/shared/guards/admin.guard.ts
+++ b/webfamily.client/src/app/shared/guards/admin.guard.ts
@@ -20,9 +20,16 @@ export class AdminGuard {
       map((user: User | null) => {
 
         if (user) {
-          const decodedToken: any = jwtDecode(user.jwt);
-          if (decodedToken.role.includes('Admin')) {
-            return true;
+          try {
+            const decodedToken: any = jwtDecode(user.jwt);
+            const roles: string[] = Array.isArray(decodedToken.role)
+              ? decodedToken.role
+              : (decodedToken.role ? [decodedToken.role] : []);
+            if (roles.includes('Admin')) {
+              return true;
+            }
+          } catch (error) {
+            console.error('Error decoding token in AdminGuard:', error);
           }
         }
 
diff --git a/webfamily.client/src/app/shared/interceptors/jwt.interceptor.ts b/webfamily.client/src/app/shared/interceptors/jwt.interceptor.ts
index 524836f..d7ef1cc 100644
--- a/webfamily.client/src/app/shared/interceptors/jwt.interceptor.ts
+++ b/webfamily.client/src/app/shared/interceptors/jwt.interceptor.ts
@@ -15,8 +15,9 @@ export class JwtInterceptor implements HttpInterceptor {
       return next.handle(request);
     }
 
-    // Skip authentication for login/register endpoints
-    if (request.url.includes('/login') || request.url.includes('/register') || request.url.includes('/account')) {
+    // Skip authentication for login/register endpoints (but not the authenticated refresh-user-token endpoint)
+    if (request.url.includes('/account/login') ||
+        request.url.includes('/account/register')) {
       return next.handle(request); // Go directly, don't check for user
     }
 
