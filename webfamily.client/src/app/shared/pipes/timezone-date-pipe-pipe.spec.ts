import { TimezoneDatePipePipe } from './timezone-date-pipe-pipe';

describe('TimezoneDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new TimezoneDatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
