import { DataSource } from '@angular/cdk/collections';
import { SearchFor } from './search-for';

describe('SearchFor', () => {
  it('should create an instance', () => {
    expect(new SearchFor()).toBeTruthy();
  });
});
